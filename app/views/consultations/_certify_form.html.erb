<%= simple_form_for @consultation.user do |f| %>
  <%= f.association :tags, label: 'Topics',  collection: @consultation.tags , as: :check_boxes %>
  <%= hidden_field_tag :consultation_id, @consultation.id %>
  <%= f.submit %>
<% end %>
